# Reached as: codex-conductor
# A narrower manifest: this harness never sees production write credentials.

bin      = $HOME/.local/bin
manifest = limited.env.tpl
labels   = no
command  = codex -s danger-full-access -a on-request
