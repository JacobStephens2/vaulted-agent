# empty.env — day-one manifest with no secrets.
#
# Auto-activated harnesses use backend=plainfile and this file so
# `vaulted-agent claude` (etc.) can launch immediately after install, before
# you wire a vault. Add KEY=value lines here for plainfile, or switch the
# harness to onepassword/bitwarden/pass/sops and point at a real template.
