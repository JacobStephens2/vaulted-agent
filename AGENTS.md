# Agent notes — vaulted-agent-launcher

## Agent skills

### Issue tracker

GitHub Issues via `gh` (repo: JacobStephens2/vaulted-agent-launcher). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` (when present) + `docs/adr/`. See `docs/agents/domain.md`.
